---
title: PbIcon
description: Centralized icon factory for Propertybox app – provides consistent, tintable SVG icons via static methods, with sensible defaults and copyWith support.
---

# PbIcon Widget

The `PbIcon` class is the single source of truth for all branded SVG icons in the Propertybox design system (used across property cards, campaign dashboards, filters, status badges, etc.).

It offers:
- Static factory methods like `PbIcon.campaign()` that return a ready-to-use `Widget` (behaves just like Flutter's built-in `Icon`)
- Default size of **15px** and **black** tint (overridable)
- Semantic defaults for status icons (e.g. green check, amber star)
- `.copyWith()` support for easy variations (size, color, fit)
- Automatic color tinting via `ColorFilter` for perfect matching with text/buttons
- All icons sourced from `assets/svgs/pb_icons/*.svg` via flutter_gen

<img
  src="https://raw.githubusercontent.com/FocalAgent-Limited/propertybox-ui/main/docs/components/pb_icon/preview-grid.png"
  alt="PbIcon gallery – campaign, floorPlan, epc, check, star, etc."
  width="480"
/>

## All Available PbIcon Icons

All icons are used like: `PbIcon.nameOfIcon(size: 20, color: Colors.blue)`

| Icon Method                  | Suggested Use Case                          | Default Color     | Typical Size Range |
|------------------------------|---------------------------------------------|-------------------|--------------------|
| `PbIcon.account()`           | User profile, account settings              | Black             | 15–24             |
| `PbIcon.camera()`            | Add photo, upload image                     | Black             | 15–28             |
| `PbIcon.search()`            | Search bar, property search                 | Black             | 15–24             |
| `PbIcon.view()`              | View details, eye icon                      | Black             | 15–20             |
| `PbIcon.comment()`           | Comments, notes                             | Black             | 15–20             |
| `PbIcon.discussion()`        | Chat, conversation thread                   | Black             | 20–24             |
| `PbIcon.writeNotes()`        | Add note, edit description                  | Black             | 20–24             |
| `PbIcon.campaign()`          | Marketing campaign, advert                  | Black             | 20–32             |
| `PbIcon.epc()`               | Energy Performance Certificate              | Black             | 24–32             |
| `PbIcon.floorPlan()`         | Floor plan view                             | Black             | 24–36             |
| `PbIcon.certificate()`       | Certification, compliance badge             | Black             | 20–28             |
| `PbIcon.check()`             | Verified, success, tick                     | Green             | 14–20             |
| `PbIcon.thumbsUp()`          | Like, approved, positive feedback           | Green             | 16–24             |
| `PbIcon.star()`              | Favourite, rating, featured                 | Amber             | 16–24             |
| `PbIcon.download()`          | Download brochure, PDF                      | Black             | 20–24             |
| `PbIcon.refreshInCircle()`   | Refresh / reload data                       | Black             | 20–28             |
| `PbIcon.syncInCircle()`      | Sync / update listings                      | Black             | 20–28             |
| `PbIcon.chevronInCircleRight()` | Next, forward, arrow in circle           | Black             | 16–24             |
| `PbIcon.subscription()`      | Subscription plan, billing                  | Black             | 24–32             |
| `PbIcon.integrations()`      | Connect integrations, API                   | Black             | 24–32             |
| `PbIcon.preferences()`       | Settings, filters                           | Black             | 20–28             |
| `PbIcon.workspace()`         | Workspace / team area                       | Black             | 24–32             |
| `PbIcon.template()`          | Email/SMS template, design template         | Black             | 24–32             |
| `PbIcon.switchButton()`      | Toggle, switch account                      | Black             | 20–24             |
| `PbIcon.spaceShip()`         | (Branding / fun element – launch feature?)  | Black             | 28–40             |
| `PbIcon.login()`             | Sign in, authentication                     | Black             | 20–28             |

### Quick copy-paste list (method names only)

```dart
PbIcon.account()
PbIcon.camera()
PbIcon.search()
PbIcon.view()
PbIcon.comment()
PbIcon.discussion()
PbIcon.writeNotes()
PbIcon.campaign()
PbIcon.epc()
PbIcon.floorPlan()
PbIcon.certificate()
PbIcon.check()
PbIcon.thumbsUp()
PbIcon.star()
PbIcon.download()
PbIcon.refreshInCircle()
PbIcon.syncInCircle()
PbIcon.chevronInCircleRight()
PbIcon.subscription()
PbIcon.integrations()
PbIcon.preferences()
PbIcon.workspace()
PbIcon.template()
PbIcon.switchButton()
PbIcon.spaceShip()
PbIcon.login()
```

## Class Definition

```dart
class PbIcon {
  PbIcon._(); // private constructor – static usage only

  // Core UI icons
  static Widget account({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  static Widget camera({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  static Widget search({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  // ... many more (comment, discussion, writeNotes, view, etc.)

  // Property & marketing icons
  static Widget campaign({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  static Widget epc({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  static Widget floorPlan({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  static Widget certificate({double size = 15.0, Color? color = Colors.black}) => /* ... */;

  // Status & feedback (with semantic colors)
  static Widget check({double size = 15.0, Color? color = Colors.green}) => /* ... */;
  static Widget thumbsUp({double size = 15.0, Color? color = Colors.green}) => /* ... */;
  static Widget star({double size = 15.0, Color? color = Colors.amber}) => /* ... */;

  // Actions & misc
  static Widget download({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  static Widget refreshInCircle({double size = 15.0, Color? color = Colors.black}) => /* ... */;
  // ... subscription, integrations, preferences, workspace, template, switchButton, spaceShip, login, etc.

  // copyWith entry point (when chaining needed)
  static PbIconBuilder base(String path, {double size = 15.0, Color? color}) => /* ... */;
}

// Optional builder for copyWith chaining
class PbIconBuilder {
  // ... implementation with copyWith() → returns new builder
  Widget build() => /* renders SvgPicture.asset */;
  Widget call() => build(); // allows direct usage as Widget
}
```

## Properties

All static methods on `PbIcon` (e.g. `PbIcon.campaign()`, `PbIcon.floorPlan()`, etc.) accept the same set of optional parameters:

- `size` (`double`, optional, default: `15.0`):  
  The width and height of the icon in logical pixels. Use larger values (e.g. 20–32) for prominent placements like buttons or cards, and smaller values (e.g. 12–16) for inline or list use.

- `color` (`Color?`, optional):  
  The tint color applied to the SVG via `ColorFilter.mode(..., BlendMode.srcIn)`.  
  - Most icons default to `Colors.black` for high contrast on light backgrounds.  
  - Status icons have semantic defaults:  
    - `check` and `thumbsUp` → `Colors.green`  
    - `star` → `Colors.amber`  
  - Commonly overridden to match brand colors (`BrandColors.primary`), button text color, or theme accents.

- `fit` (`BoxFit`, optional, default: `BoxFit.contain`):  
  Controls how the SVG content fits within its bounding box. Rarely changed — `contain` works best for most icons in the Propertybox design system.

## Usage Examples
### Basic usage (inline like Icon widget)
```dart
Row(
  children: [
    PbIcon.campaign(size: 20),
    const SizedBox(width: 8),
    PbIcon.floorPlan(color: BrandColors.primary),
    PbIcon.check(), // uses green by default
  ],
)
```

### In buttons & cards (auto-tinted by parent)
```dart
PbButton(
  style: PbButtonStyle.secondary,
  label: 'View Floor Plan',
  icon: PbIcon.floorPlan(), // auto-tinted to button text color
  secondaryColor: PbSecondaryColor.teal,
  onPressed: _openFloorPlan,
)
```

### Creating variations with copyWith
```dart
final baseCampaign = PbIcon.campaign();

Column(
  children: [
    baseCampaign.copyWith(size: 32, color: Colors.blueGrey)(),
    baseCampaign.copyWith(size: 12, color: Colors.grey.shade500)(),
  ],
)
```